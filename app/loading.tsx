import Loader from "@/app/components/Loader"; // Import the Loader component.

// Define the Loading component, which displays a loader.
const Loading = () => {
    return (
        <Loader />
    );
}

export default Loading; // Export the Loading component.
